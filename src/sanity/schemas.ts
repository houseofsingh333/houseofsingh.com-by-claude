/**
 * Sanity schema type stubs.
 * When Sanity is set up, these will become actual schema definitions
 * in a /sanity/schemas/ folder. For now this documents the planned shapes.
 *
 * Schemas to implement:
 *   - heroSlide
 *   - project
 *   - journalEntry
 *   - contactSubmission
 *   - siteSettings (singleton)
 *   - navigation (singleton)
 */

export const schemaTypes: string[] = [
  "heroSlide",
  "project",
  "journalEntry",
  "contactSubmission",
  "siteSettings",
  "navigation",
];
